kind = "web"
previewPath = "/"
title = "AZ Distribution Invoice Manager"
version = "1.0.0"
id = "artifacts/invoice-manager"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18864

[services.development]
run = "pnpm --filter @workspace/invoice-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/invoice-manager", "run", "build" ]
publicDir = "artifacts/invoice-manager/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18864"
BASE_PATH = "/"
